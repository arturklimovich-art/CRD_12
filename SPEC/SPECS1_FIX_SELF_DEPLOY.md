\# Паспорт задачи: S1-FIX-SELF-DEPLOY



\*\*Alias:\*\* `S1-FIX-SELF-DEPLOY`  

\*\*Название:\*\* Проверка и завершение реализации Self-Deploy \& Auto-Recovery Fix  

\*\*Статус:\*\* ✅ DONE (после верификации)  

\*\*Ответственный:\*\* `engineer\_b\_api`  

\*\*Связан с:\*\* `E1-L3`, `E1-L4`, `Navigator DB`, `Bot (Orchestrator)`, `Curator`  

\*\*Входит в:\*\* пост-стабилизацию Этапа 1, обязательное завершение перед L2.



---



\## 🧩 Архитектура



\### Основные компоненты:

\- \*\*`engineer\_b\_api`\*\* — сервис, принимающий патчи, запускается под `supervisor.py` (PID 1).

\- \*\*`supervisor.py`\*\* — менеджер жизненного цикла, перезапускает воркер при необходимости.

\- \*\*`runtime\_smoke.py`\*\* — модуль пред-валидации патчей (внедрён в `app.py`, требует выноса).

\- \*\*`core\\event\_bus.py`\*\* — отправка событий в `core.events`.

\- \*\*`Bot (Orchestrator)`\*\* — инициирует задачи, ожидает `post\_deploy\_validated`.

\- \*\*`Curator`\*\* — контролирует инварианты и политику отката.

\- \*\*`Navigator DB`\*\* — хранит статусы задач (`eng\_it.tasks`), артефакты, метрики.



---



\## 🧭 Пути проекта



