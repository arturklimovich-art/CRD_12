# Agent Tasks (Console A)
- Submit‑TZ, Pack‑Patch, Run‑Sandbox (lint/static/smoke), Snapshot/Restore
- Request‑Apply (to Engineers_IT), Status‑Panel, Queue/History/Logs helpers
- Each step emits events to core.events; failures include rationale
