# Readmap Bootstrap
Steps: create directories (ROADMAP/, JOURNAL/, workspace/readmap/revisions/),
place ROADMAP.{yaml,md}, run Readmap‑Load (schema+checksum), append revision,
emit readmap.loaded, set Navigator baseline, enable CI Spec‑Guard.
