# Local SFT (LoRA/QLoRA) for 6GB
- Dataset curation (dialogs/ADR, 'teacher' style), cleaning
- Axolotl/LLaMA‑Factory; small batch + grad accumulation
- A/B 20–30 prompts; enable if ≥70% preference; store adapter configs
