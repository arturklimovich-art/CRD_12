# EngineersIT.Bot

PowerShell модуль для автоматизации работы с системой CRD_12.

## 🚀 Быстрый переход между чатами

### Проблема

При переходе в новый чат с Copilot теряется контекст предыдущей сессии. Вход в 100% контекст занимает **25-35 ответов** и достигается только к **50 ответу**, что занимает **10-15 минут**.

### Решение

Система автоматизации переходов между чатами позволяет восстановить 100% контекста за **1-2 команды (30 секунд)**.

---

### 📝 Завершение текущего чата

Перед закрытием чата выполни:

```powershell
powershell -File "agents\EngineersIT.Bot\Generate-Context-Enhanced.ps1" `
    -LastTask "E2-TL-B0-T3" `
    -SessionSummary "Завершена синхронизация roadmap домена TL"
```

### 🚀 Начало нового чата

В новом чате выполни **одну команду**:

```powershell
powershell -File "agents\EngineersIT.Bot\Quick-Start.ps1"
```

Затем вставь контекст в чат (`Ctrl+V`) — готово! 🎉

### 📊 Результат

| Метрика | Раньше | Сейчас |
|---------|--------|--------|
| Вход в контекст | 25-35 ответов | **1 команда** ✅ |
| 100% понимание | К 50 ответу | **Мгновенно** ✅ |
| Время на восстановление | 10-15 минут | **30 секунд** ✅ |

---

## 📚 Документация

### Generate-Context-Enhanced.ps1

Генерирует полный JSON контекст сессии, включающий:
- Метаданные сессии (пользователь, timestamp, последняя задача)
- Структуру проекта (репозиторий, ветка, контейнеры, БД)
- Состояние roadmap (блоки, задачи, шаги с детализацией)
- Последние изменения из БД
- Известные проблемы
- Следующие задачи
- Ключевые команды

**Параметры:**
- `-OutputFile` - имя файла для сохранения (по умолчанию: `context_latest.json`)
- `-User` - имя пользователя (по умолчанию: `arturklimovich-art`)
- `-LastTask` - код последней выполненной задачи
- `-SessionSummary` - резюме сессии
- `-DBHost` - хост БД (по умолчанию: `localhost`)
- `-DBPort` - порт БД (по умолчанию: `5433`)
- `-DBName` - имя БД (по умолчанию: `crd12`)
- `-DBUser` - пользователь БД (по умолчанию: `crd_user`)
- `-DBPassword` - пароль БД (по умолчанию: `crd12`)

### Load-Context.ps1

Загружает JSON контекст и:
- Выводит красиво отформатированную информацию в консоль с цветами
- Генерирует Markdown для вставки в чат
- Копирует Markdown в буфер обмена

**Параметры:**
- `-ContextFile` - имя файла контекста (по умолчанию: `context_latest.json`)

### Quick-Start.ps1

Единая команда для полного восстановления контекста:
1. Загружает SoT-Commands
2. Синхронизирует roadmap
3. Загружает контекст
4. Проверяет домены

**Параметры:**
- `-ContextFile` - имя файла контекста (по умолчанию: `context_latest.json`)

### known_issues.json

JSON файл с известными проблемами системы. Используется в `Generate-Context-Enhanced.ps1` для включения в контекст.

Формат:
```json
[
  {
    "issue": "Описание проблемы",
    "status": "workaround_applied|open|resolved",
    "workaround": "Решение проблемы",
    "discovered_at": "2025-11-24T11:50:00Z",
    "affected_components": ["компонент1", "компонент2"],
    "severity": "low|medium|high|critical"
  }
]
```

---

## 🔧 Системные требования

- PowerShell 5.1+ (Windows) или PowerShell Core 7+ (Linux/macOS)
- Docker с запущенными контейнерами `crd12_engineer_b_api` и `crd12_pgvector`
- PostgreSQL база данных `crd12` на порту 5433
- Git репозиторий CRD_12

---

## 📋 Лицензия

Внутренний инструмент проекта CRD_12.
